import { useEffect, useState } from 'react';
import PerformanceCard from '../../components/performance/PerformanceCard';
import WeekPicker from '../../components/performance/WeekPicker';
import { api, apiErrorMessage } from '../../services/api';

function mondayValue(){const d=new Date();const day=d.getUTCDay();d.setUTCDate(d.getUTCDate()+(day===0?-6:1-day));d.setUTCHours(0,0,0,0);return d.toISOString().slice(0,10);}

export default function ParentOverviewPage(){ const [weekStart,setWeekStart]=useState(mondayValue); const [data,setData]=useState(null); const [error,setError]=useState(''); useEffect(()=>{setError('');api.get('/parent/overview',{params:{weekStart}}).then(({data})=>setData(data)).catch((err)=>setError(apiErrorMessage(err,'Could not load parent dashboard.')));},[weekStart]); return <main className="px-4 py-7 sm:px-6 lg:px-8 lg:py-9"><div className="mb-7 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="mb-2 text-[0.72rem] font-extrabold uppercase tracking-[0.1em] text-bastly-blue">Parent dashboard</p><h1 className="mb-2 font-heading text-[clamp(2rem,5vw,3.3rem)] font-bold tracking-[-0.055em] text-bastly-navy">Their week at a glance.</h1><p className="mb-0 max-w-[720px] text-sm leading-6 text-muted">See every linked child's course performance without waiting for weekly emails.</p></div><WeekPicker value={weekStart} onChange={setWeekStart}/></div>{error&&<div className="mb-4 rounded-2xl bg-[#fff0ef] px-4 py-3 text-sm font-bold text-[#a83d36]">{error}</div>}<div className="grid gap-7">{(data?.children||[]).map((child)=><section key={child.relationshipId}><div className="mb-3"><p className="mb-0 font-heading text-2xl font-bold text-bastly-navy">{child.student.fullName}</p><p className="mb-0 text-xs text-muted">{child.student.email}</p></div><div className="grid gap-4 lg:grid-cols-2">{child.courses.map((row)=><PerformanceCard key={row.enrollmentId} course={row.course} group={row.group} performance={row.performance}/>)}</div>{child.courses.length===0&&<div className="rounded-2xl border border-line bg-white p-5 text-sm text-muted shadow-soft">No active course performance for this week.</div>}</section>)}{data&&data.children?.length===0&&<div className="rounded-[24px] border border-line bg-white p-10 text-center text-sm text-muted shadow-soft">No active child links were found for this parent account.</div>}</div></main>; }
